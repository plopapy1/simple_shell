#include <stdio.h>
#include <unistd.h>

/**
  * main - simple additcion
  *
  * Return: reture zero (0)
  *
  */

int main(void)
{
	int a = 3;
	int b = 4;
	int c;

	if (a != 0)
	{
		c = b + a
		printf("c equal %d\n" c);
	}

	return(0)
}
